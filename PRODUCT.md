# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要使用者：繁體中文圈的 NAS 自架玩家。**

處境：想看的內容散在各家串流平台，於是自己架了 NAS 當家庭劇院。片子收得到，但**網路上的資源多數沒有繁中字幕**——片庫有了，體驗缺一塊。熟悉 Docker、能自己編輯 `.env`、願意為 AI 功能自備 API key（並接受用量計費）。

要完成的工作：**讓整個片庫自動有可看的繁中字幕，而且不用盯著它跑。** 不是「幫我找一次字幕」，是「我不在的時候你自己處理完」。

每一份安裝都是**單人使用**（無登入機制，v4 明確決定）——但「單人」指的是每台機器一個人，不代表只有作者一個人用。2026-08-25 確認：目標是這個小眾，不是個人工具。

作者本人是第一個使用者，也是目前唯一在正式環境跑的人。

## Product Purpose

把「這部片沒有繁中字幕」這件事做到底，用一條有優先序的路徑：

```
影片內嵌字幕  →  線上字幕來源  →  ASR 語音辨識
（開發中）        （射手網 / OpenSubtitles）   （前兩者都沒有時，聽聲音生字幕再翻）
```

字幕解決之後，才長出媒體庫的其他部分（繁中 metadata、字幕組檔名解析、下載監控、活動中心）——那些的存在理由是「讓這個家庭劇院真的能用」，不是獨立目標。

**成功長什麼樣**：一個不是作者的人，能自己把它裝起來、指到自己的片庫、離開，回來時字幕已經好了——而且他相信畫面上顯示的狀態是真的。

## Positioning

已於 2026-08-19 策略重審中對抗性查證（22 個獨立分析代理，主張均經 repo file:line 與網路來源實地驗證）。

**護城河不在字幕管線本身。** 同型態的無人值守 ASR+LLM 管線已被獨立復刻多次；「Bazarr 抽內嵌 → Lingarr 翻譯」是現役的 DIY 組合。管線同位是易腐資產。

站得住的是三層：

1. **值得信任的無人值守閉環（核心）** — 單一畫面的誠實狀態 ＋ 花費上限 ＋ 同意流程。多容器 DIY 組裝在**結構上**做不到跨工具的單一真相；克隆者會跳過信任機制（demo 看不見，工程量是管線的 5–10 倍）。
2. **逐庫累積的狀態** — 詞彙庫、校正史、TMDb 修正、segment cache。唯一隨 AI 模型變強而更強的資產。是轉換成本，不是進入障礙。
3. **法律定位 ＋ 真空卡位** — ChineseSubFinder（繁中 NAS 字幕的前任）因爬字幕源的法律風險與 burnout 停更。Vido 的「生成不下載」架構對該死因**結構免疫**：字幕來源只走官方 keyed API，不加種子，\*arr 串接走使用者自有實例。

**一句話**：工具需要操作者，Vido 移除操作者 —— 而「移除操作者」要成立，前提是狀態誠實（不然無人值守＝沒人發現它在騙你）。

## Operating Context

- **部署**：單一 Docker 容器跑在使用者自己的 NAS 上。媒體以**唯讀**掛載。透過區網瀏覽器操作。
- **裝置（2026-08-25 確認）**：**手機與桌機同等重要**。不是「桌機操作、手機唯讀」——手機上要能完成完整任務，不只是查看進度。
- **時間尺度**：ASR 與翻譯是以分鐘計的背景工作。使用者會關掉視窗走開，之後回來查。「關掉之後還在跑」是核心期待，不是附加功能。
- **金錢**：AI 功能走雲端 API，**依用量真實計費**（`AI_RUN_BUDGET_USD` 預設 5.0 為單次上限）。花費可見與事前同意是使用場景的一部分，不是選配。
- **降級是常態**：多數使用者不會設滿所有 API key。沒有金鑰時該功能停用，其餘照常運作。

## Capabilities and Constraints

**已可用**：線上字幕搜尋與下載（射手網／OpenSubtitles，自動評分）、簡轉繁（opencc）、AI 字幕增強（詞彙庫維持譯名一致）、ASR 生字幕（Whisper）、字幕軌偵測、繁中優先 metadata（TMDB zh-TW → 豆瓣 → Wikipedia，後兩者預設關閉）、AI 字幕組檔名解析（含學習機制）、qBittorrent 下載監控、媒體庫掃描與多資料夾、瀏覽／進階搜尋／詳情頁、活動中心、首次啟動 setup wizard。

**開發中**：抽取內嵌字幕來翻譯（目前只做到偵測有哪些軌）、Request 系統（一鍵「想要」＋ Radarr/Sonarr）。

**已知限制**：

- ASR **目前僅支援電影**，影集入口開發中；靜音片段偶爾產生幻覺字幕（VAD 過濾開發中）。
- 每份安裝單人使用，無登入機制（v4 決定，多人延後至 v5）。
- 大陸出品內容**保留簡體字幕不轉換**（用 TMDb `production_countries` 判斷）——對白用語與配音相符。

**技術約束**：Go 1.25 + Gin + SQLite（WAL + FTS5）後端；React 19 + TanStack Router/Query + Tailwind CSS v4 前端；單容器部署，Go 直接serve 靜態檔案（無 Nginx）。

**明確未決**（未來工作不得假裝已存在）：是否發布正式 release、是否推送公開 Docker image、採用什麼授權條款。目前三者皆無。

## Brand Commitments

- **名稱**：Vido。介面左上為小寫 wordmark `vido`，副標「NAS 媒體庫」。
- **語言**：繁體中文為主。使用者可見文件需 EN + zh-TW 雙語版本（專案 Rule 17）。
- **語氣：誠實優先於好看。** 這是產品定位的直接延伸，不是風格偏好——不編造百分比、不用綠色宣稱一件沒在發生的事、系統與使用者意願不一致時要講清楚是誰沒啟用。已有多次裁定實例（琥珀色＝你要求了但沒發生；不報假進度而顯示「進行中」）。

## Evidence on Hand

**真實可用**：

- 作者的 Unraid NAS 正式實例（`192.168.50.52:8088`），真實片庫：55 部電影、2406 集。
- `ux-design.pen` 設計檔 ＋ `_bmad-output/screenshots/` 已匯出 162 張畫面（含 A–L 各 flow 與 J 系列決策 spec 頁）。
- 對抗性查證過的競品／定位分析：`_bmad-output/planning-artifacts/strategy-review-whatsub-2026-08-19.md`。
- 完整的 BMAD 規劃文件、architecture ADR、sprint 追蹤紀錄。

**明確不存在——未來工作不得捏造**：

- 沒有正式 release、沒有公開 Docker image、沒有授權條款。
- 沒有使用者見證、沒有客戶、沒有下載數、沒有 star 數可引用。
- 沒有營收。12 個月內月營收 >US$500 已判定不可行（信心 0.85，經對抗查證）——定價／方案頁不應存在。

## Product Principles

1. **誠實勝過好看的數字** — 沒有真實進度就顯示「進行中」，不顯示會被讀成卡住的 0%。不追蹤的東西不報數字。
2. **移除操作者** — 預設無人值守。需要人盯著的設計就是失敗的設計。
3. **花錢的事先問** — 成本在花掉之前可見且經過同意，有上限、可中止。
4. **繁中是目的地，不是選項** — 每條路徑的終點都是可看的繁中字幕。
5. **降級不失效** — 缺金鑰或服務不可用時停用那個功能，不是讓整頁壞掉。

## Accessibility & Inclusion

- **對比度：WCAG AA 為硬性要求**（內文 4.5:1）。2026-08-25 確認。已有實例：`--text-disabled` 實測 3.55:1，被判定不可用於 12px 說明文字。
- **鍵盤操作與螢幕閱讀器**：盡力而為，不設為硬性閘門。
- **行動裝置**：手機必須能完成完整任務，不是唯讀降級版——這是使用場景的事實，也是無障礙的一部分。
