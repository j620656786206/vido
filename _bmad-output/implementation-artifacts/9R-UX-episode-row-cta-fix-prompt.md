# 9R-UX MUST-FIX —— 第二輪 Inline Agent 提示詞（很短）

> 只有一件事要改。複製 `---` 之間那段。

---

你正在編輯 `ux-design.pen`。上一輪的產出有一個必須修正的地方。

## 背景

上一輪在 `VNeL9`（`sec-eps`）裡加了一個規格註記 `p7kBBH`（`note-subtitle-entry`），
它把 `N2fmG6`（B4p-D）的高度從 900 撐到 1000。

但這個檔案裡**每一個桌面畫面都是 900**——包括 B4p-D 的四個同族兄弟
（`uRGu2` B3p-D、`Z42zy` B6p-D、`Tqy3E` B7p-D、`UH0sk` B8p-D）。
高度是「視窗尺寸」的約定，不是可以個別調整的畫布。而且那三行規格內容，
本來就該住在剛建好的獨立規格頁 `Z54xAd`（J3-D）裡。

## 要做的事（三步，照順序）

**步驟 1** —— 在 `Z54xAd`（J3-D）的裁定框 `ruling-line-3`（`M4IGxE`）之後，
補兩行同款式的裁定行（13px、`$text-secondary`、`Noto Sans TC`）：

- `可及性名稱必須含集號，例如「管理 S01E03 的字幕」——同頁十餘列文案相同，沒有集號無法區辨。`
- `行動版（<sm）：整列改為上下堆疊（沿用 12-2 的堆疊規則），管理字幕移到第二行、靠左、維持 44px 觸控高度；不另出 TV 行動版畫面。`

（原註記的第一行不必搬——`ruling-line-1`、`ruling-line-3` 與十列表格已經完整涵蓋。）

**步驟 2** —— 刪除 `p7kBBH`（連同其四個子節點）。

**步驟 3** —— 把 `N2fmG6` 的 `height` 改回 `900`。

## 完成後

確認 `N2fmG6` 沒有任何內容被裁切（刪掉註記後應該剛好容得下），然後**存檔**。

匯出只需重產兩張：`b4p-d`、`j3-d`。`f1-d-v2` 這一輪沒動，**不要**動它的截圖。

---
