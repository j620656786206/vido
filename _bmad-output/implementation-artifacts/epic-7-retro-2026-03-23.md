# Epic 7 Retrospective: Media Library Scanner

**Date:** 2026-03-23
**Epic:** 7 — Media Library Scanner
**Status:** Complete (4/4 stories done)
**Facilitator:** Bob (Scrum Master) — updated post-verification

---

## Epic Summary

| Metric | Value |
|--------|-------|
| Stories Completed | 4/4 (100%) |
| Total Tests Added | 47 (scanner) + Go tests |
| Total Test Suite | 1503+ passing (frontend) + Go passing |
| Commits | 12 across all stories |
| TA Scores | 7-1: 82, 7-3: 82, 7-4: 82 |
| CR Issues Found/Fixed | 7-3: 5/5, 7-4: 5/5 |
| Production Incidents | 0 |
| Technical Debt Items | 2 (LOW severity from 7-4 CR) |

### Stories Delivered

1. **7-1: Recursive Folder Scanner** — Go backend: walker, file discovery, SSE hub, scan endpoints
2. **7-2: Scheduled Scan Service** — Cron scheduling, incremental scan (mtime), mutex locking
3. **7-3: Manual Scan Trigger UI** — Settings page, TanStack Query hooks, polling scan status
4. **7-4: Scan Progress Tracking** — SSE EventSource hook, desktop card, mobile sheet, cancel flow

---

## What Went Well

### 1. Backend-First Architecture Paid Off
Stories 7-1 and 7-2 built a solid Go backend foundation (SSE hub, scanner service, REST endpoints) before any frontend work. When 7-3 and 7-4 needed to consume the API, everything was already available and tested. Zero backend changes needed for frontend stories.

### 2. Consistent Quality Gate Process
Every story went through TA + CR + Fix. All stories scored 82/100 on TA (A - Good). The CR process caught real issues:
- 7-3: Broken test assertion, setTimeout memory leak, missing hook tests
- 7-4: SSE reconnection gap, non-functional action links, useEffect stability

### 3. First SSE Integration Successful
Story 7-4 delivered the project's first EventSource-based real-time UI. The pattern (useReducer + SSE events + polling fallback) is now established and reusable for Epic 8 (subtitle progress) and beyond. The MockEventSource test helper is a valuable test utility.

### 4. Mobile-First Responsive Design
Both 7-3 (settings) and 7-4 (progress) shipped with proper mobile variants. The bottom sheet pattern with peek/expanded states and touch gestures is production-ready.

### 5. Design-to-Code Pipeline
Flow H UX design (8 screens) was completed and exported as screenshots before frontend implementation began. This gave clear targets for Stories 7-3 and 7-4.

---

## What Could Be Improved

### 1. Formatting/Lint Not Caught Pre-Commit
CI failed on Prettier formatting. The `.husky/pre-commit` hook isn't executable, so formatting issues slip through to CI. This wasted a CI cycle.

**Action Item:** Fix husky pre-commit hook permissions (`chmod +x .husky/pre-commit`) or add format-on-save to editor config.

### 2. SSE Event Schema Gap
The `ScanProgressEvent` type in the spec had `filesFound` (camelCase) but the actual backend sends `files_found` (snake_case). The story spec also lists `files_parsed` and `files_matched` fields that don't exist in the backend yet. This caused confusion in implementation and required the `filesProcessed` estimation workaround.

**Action Item:** When writing Story specs that reference API schemas, always verify field names against the actual Go struct JSON tags.

### 3. Duplicated Test Fixtures
ScanProgressCard and ScanProgressSheet tests both define identical `baseScanningState`, `completeState`, and `cancelledState` objects. This was flagged in TA review but deferred.

**Action Item:** Extract shared test fixtures to `__tests__/fixtures.ts` when more scanner tests accumulate.

### 4. No Story for Backend SSE Events
The backend sends `scan_progress` events, but there's no `scan_complete` or `scan_cancelled` event type defined in `hub.go`. Story 7-4 frontend listens for these events, but the backend doesn't emit them yet. The completion is detected via polling (STATUS_UPDATE when `is_scanning` flips to false).

**Action Item:** Add `EventScanComplete` and `EventScanCancelled` event types to `sse/hub.go` constants and emit them from the scanner service. This should be a tech debt story in Epic 8.

### 5. SSE Persistent Connection Broke All E2E Tests (Critical Post-Merge Discovery)
The initial `useScanProgress` hook created an EventSource connection **unconditionally on mount**. Since `ScanProgress` is in the root layout, every page load opened a persistent SSE connection to `/api/v1/events`. This prevented Playwright's `networkidle` state from ever resolving, causing **all UI E2E tests across shards 2/3/4 to timeout** (30s each, 3 retries). Only API-only tests in shard 1 passed.

**Root Cause:** EventSource maintains a persistent HTTP connection. Playwright's `networkidle` waits for 0 open connections for 500ms — impossible with SSE always open.

**Fix Applied:** Changed to lazy SSE — only connects after initial status check confirms scan is active. Idle poll (10s) detects scans started from settings/schedule. This is a **critical pattern** for any future SSE consumer added to globally-mounted components.

**Lesson Learned:** Any persistent connection (SSE, WebSocket) in a root-level component MUST be lazy-initialized to avoid breaking E2E test infrastructure. This should be documented in project-context.md.

### 6. UX Verification Uncovered Design Drift
Task 5.6 UX verification against design screenshots found 3 discrepancies that passed CR unnoticed:
- Stats row had 3 counters (找到/解析/錯誤) but design spec H2/H5 requires 4 (找到/解析/比對/錯誤 with Lucide Link icon)
- Completion toast was positioned bottom-right but H3 spec says top-center (480px max-width)
- Completion card used radius-xl but H3 spec says radius-lg

**Lesson Learned:** UX verification (Task 5.6) catches issues that code review misses. The CR focused on code quality/architecture but didn't cross-reference the design brief element-by-element. UX verification should be done BEFORE CR, not after.

---

## Key Technical Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| useReducer for SSE state (not TanStack Query) | SSE is push-based, not request-response | Clean state machine, easy to test |
| Polling fallback on SSE error | Browser EventSource reconnects, but we need faster recovery | Works well, reconnects after 10s |
| 768px breakpoint for card vs sheet | Matches existing responsive patterns | Consistent with app shell |
| Auto-dismiss with shrinking progress bar | UX spec requirement, 10s timer | Smooth animation, pausable on hover |
| filesProcessed estimated from percentDone | Backend SSE doesn't send this field | Acceptable approximation |
| Lazy SSE (connect only when scanning) | Persistent SSE broke E2E networkidle | Critical fix — must be default pattern |
| Completion toast top-center (not bottom-right) | H3 design spec position | Better UX — toast doesn't overlap content area |

---

## Impact on Epic 8 (Subtitle Engine)

### Dependencies Met
- SSE hub is operational and tested (hub.go + handler.go)
- EventSource pattern established in frontend (useScanProgress.ts)
- ScanProgressCard component pattern can be cloned for subtitle progress
- MockEventSource test helper is reusable

### Preparation Needed for Epic 8
1. **Add missing SSE event types** — `scan_complete`, `scan_cancelled` should be real backend events
2. **Subtitle fields migration** — Already done in Phase 0 (0-1)
3. **Repository extensions** — Already done in Phase 0 (0-2)
4. **SSE event pattern** — Established, can extend with `EventSubtitleProgress`

### Risks
- Subtitle processing may be much slower than scanning (network calls to external APIs)
- May need to queue subtitle jobs differently than scan jobs
- Progress tracking granularity may differ (per-file vs per-subtitle-source)

---

## Action Items

| # | Action | Owner | Priority | Target | Status |
|---|--------|-------|----------|--------|--------|
| 1 | Fix husky pre-commit hook permissions | Dev | HIGH | Before Epic 8 starts | Open |
| 2 | Add EventScanComplete/Cancelled to backend | Dev | MEDIUM | Epic 8 tech debt story | Open |
| 3 | Extract shared scanner test fixtures | Dev | LOW | When adding more scanner tests | Open |
| 4 | Replace auto-dismiss setInterval with CSS animation | Dev | LOW | Backlog | Open |
| 5 | Document lazy SSE pattern in project-context.md | Dev | HIGH | Before Epic 8 starts | Open |
| 6 | Run UX verification BEFORE CR in dev-story workflow | SM | MEDIUM | Process improvement | Open |

---

## Team Reflections

**Velocity:** All 4 stories completed in a single day (2026-03-23), including UX design, implementation, TA, CR, and fixes. The BMAD workflow + AI agent pipeline is highly efficient for this project.

**Quality:** Consistent 82/100 TA scores across stories. CR process caught meaningful issues (not just style nits). All fixes verified with passing tests before commit.

**Process:** The session handoff document pattern works well for multi-session continuity. Story specs with design screenshot references streamline frontend implementation.

**Post-Merge Discovery:** The lazy SSE fix is the most impactful lesson from this epic. It wasn't caught in unit tests (which don't use `networkidle`) or CR (which focused on code logic). Only CI E2E revealed it. This reinforces the value of full CI pipeline — lint + unit + build + E2E — all must pass.

---

## Conclusion

Epic 7 delivered the complete "point at your folders and go" experience: folder configuration, manual/scheduled scanning, and real-time progress tracking with cancel support. The SSE infrastructure is now proven and ready for reuse. The project is well-positioned to begin Epic 8 (Subtitle Engine).

**Next Priority:** Epic 8 — Subtitle Engine
