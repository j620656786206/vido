# Auto Claude data directory
.auto-claude/

# Claude Code TTS audio files
.claude/audio/

# TestSprite temp files (contains API keys and session data)
testsprite_tests/tmp/

# API runtime data (posters cache, DB files)
apps/api/vido-data/

# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
out-tsc

# dependencies
node_modules

# Environment files
.env
.env.local
.env.*.local

# Go build artifacts
bin/
*.exe
*.dll
*.so
*.dylib
*.test
*.out

# Go compiled binaries (without extension)
apps/api/api
apps/api/main
apps/*/api

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
*.swp
*.swo
*~

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# SQLite data directory
apps/api/data/

# Air (hot reload) tmp directory
tmp/

# Vendor directory
vendor/

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data

vite.config.*.timestamp*
vitest.config.*.timestamp*

# Playwright
test-results/
playwright-report/
playwright/.cache/
blob-report/
.nx/polygraph
.claude/worktrees
.claude/settings.local.json
.claude/projects

# Pencil auto-backups of the design file (e.g. ux-design.pen.bak-imports-YYYYMMDD-HHMMSS)
ux-design.pen.bak-*
# Local test environment (scripts/serve-test-env.sh — testenv-local-seed)
.vido-test-env/

testsprite_tests/tmp/config.json

# 本機產出的截圖與文章草稿(不進版控)
output/
artifacts/sbom/
.claude/github-star-reminder.txt

# eval/: full subtitle transcripts from the blind eval stay on the NAS (/mnt/user/appdata/vido/eval/),
# never in this public repo. Scripts, key.json, aggregated scores and zero-score excerpts are tracked.
eval/**/*.srt
eval/**/*.ass
eval/**/sheet*.csv
eval/**/compare.csv
