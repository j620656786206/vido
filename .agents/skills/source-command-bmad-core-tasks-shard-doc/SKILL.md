---
name: 'source-command-bmad-core-tasks-shard-doc'
description: 'Splits large markdown documents into smaller, organized files based on level 2 (default) sections'
---

# source-command-bmad-core-tasks-shard-doc

Use this skill when the user asks to run the migrated source command `bmad-core-tasks-shard-doc`.

## Command Template

# Shard Document

LOAD and execute the task at: \_bmad/core/tasks/shard-doc.xml

Follow all instructions in the task file exactly as written.

## MANUAL MIGRATION REQUIRED

Migrated from source command `bmad-core-tasks-shard-doc` into a Codex skill. Invoke it as `$source-command-bmad-core-tasks-shard-doc` and manually rewrite any slash-command behavior that depended on provider-specific runtime expansion.
