# /ship pipeline smoke test

This is a disposable file created to verify the `/ship` skill's end-to-end flow
(adversarial review → new branch off main → conventional commit → PR → CI).

Safe to delete. If you are reading this in a merged commit, the smoke-test PR was
merged by mistake — remove this file.
